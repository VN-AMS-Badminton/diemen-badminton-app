# Copy these values to your local environment file for development
# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SECRET_KEY=

# Session (generate with: openssl rand -base64 32)
SESSION_SECRET=

# Payment
# Active payment provider: "tikkie" (default) or "bunq". Flip to "bunq" to
# switch players to the bunq.me link; flip back for instant rollback.
PAYMENT_PROVIDER=tikkie
TIKKIE_DEFAULT_URL=https://tikkie.me/pay/example
BUNQ_DEFAULT_URL=https://bunq.me/example

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Web Push (VAPID) — generate keys with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com
