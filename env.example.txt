# Copy these values to your local environment file for development

# Supabase
NEXT_PUBLIC_SUPABASE_URL=           # Project URL from Supabase dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY= # Publishable (anon) API key — safe to expose in browser
SUPABASE_SECRET_KEY=                # Secret (service-role) key — server-side only, never expose to client

# Session (generate with: openssl rand -base64 32)
SESSION_SECRET=

# Payment
TIKKIE_DEFAULT_URL=https://tikkie.me/pay/example  # Your personal Tikkie payment link

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000  # Public base URL of the app (no trailing slash)

# Web Push (VAPID) — generate keys with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=  # VAPID public key — sent to browser for push subscription
VAPID_PRIVATE_KEY=             # VAPID private key — server-side only
VAPID_SUBJECT=mailto:admin@example.com  # Contact URI for push service (mailto: or https:)
